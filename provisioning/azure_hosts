[reverse_proxy_hosts]
jumpbox.azure

[ldap_hosts]
jumpbox.azure

[gitlab_hosts]
docker1.azure

[jenkins_masters_hosts]
docker2.azure

[jenkins_slaves_hosts]
docker3.azure

[nexus_hosts]
docker1.azure

[sonar_hosts]
docker1.azure
